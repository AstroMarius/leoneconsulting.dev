---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout 
  title="Audit Asincrono Gratuito in 48 Ore | Leone Consulting"
  description="Feedback tecnico sulla tua idea in 48 ore. Gratuito, nessun impegno. Invia brief e materiali, ricevi valutazione di fattibilità, rischi e quick wins."
>
  <Header />
  
  <main>
    <!-- Hero Section -->
    <section class="pt-32 pb-12 px-6 bg-gradient-to-br from-green-50 to-white">
      <div class="container mx-auto">
        <div class="max-w-4xl mx-auto text-center">
          <div class="inline-block px-4 py-2 bg-green-100 text-green-800 rounded-full font-semibold mb-6">
            ✨ Gratuito - Nessun Impegno
          </div>
          <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6 leading-tight">
            Feedback Tecnico sulla Tua Idea
            <span class="text-green-600">in 48 Ore</span>
          </h1>
          <p class="text-xl text-gray-600 leading-relaxed">
            Invia brief, repo o screenshot. Ricevi valutazione di fattibilità, rischi identificati e quick wins actionable. Tutto in formato scritto.
          </p>
        </div>
      </div>
    </section>

    <!-- How It Works -->
    <section class="py-16 px-6">
      <div class="container mx-auto max-w-5xl">
        <h2 class="text-3xl font-bold text-gray-900 text-center mb-12">Come Funziona</h2>
        
        <div class="grid md:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
              </svg>
            </div>
            <h3 class="font-bold text-gray-900 mb-2">1. Invia Materiali</h3>
            <p class="text-gray-600 text-sm">Brief, link repo, screenshot, sample dati</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
              </svg>
            </div>
            <h3 class="font-bold text-gray-900 mb-2">2. Analizzo</h3>
            <p class="text-gray-600 text-sm">Valuto architettura, dati, vincoli</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
            <h3 class="font-bold text-gray-900 mb-2">3. Ricevi Feedback</h3>
            <p class="text-gray-600 text-sm">Documento PDF entro 48h</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </div>
            <h3 class="font-bold text-gray-900 mb-2">4. Decidi</h3>
            <p class="text-gray-600 text-sm">Call, audit completo o procedi da solo</p>
          </div>
        </div>
      </div>
    </section>

    <!-- What You Get -->
    <section class="py-16 px-6 bg-gradient-to-br from-green-50 to-emerald-50">
      <div class="container mx-auto max-w-4xl">
        <h2 class="text-3xl font-bold text-gray-900 text-center mb-12">Cosa Ottieni</h2>
        
        <div class="grid md:grid-cols-2 gap-6">
          <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="font-bold text-gray-900 mb-2 flex items-center gap-2">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Valutazione di Fattibilità
            </h3>
            <p class="text-gray-600 text-sm">Go/No-Go/Go con modifiche, con motivazioni tecniche</p>
          </div>

          <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="font-bold text-gray-900 mb-2 flex items-center gap-2">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
              </svg>
              Rischi Identificati
            </h3>
            <p class="text-gray-600 text-sm">Top 3-5 rischi tecnici con severity e mitigazioni</p>
          </div>

          <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="font-bold text-gray-900 mb-2 flex items-center gap-2">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
              Quick Wins
            </h3>
            <p class="text-gray-600 text-sm">2-3 azioni immediate (1-2 sett) con alto impatto</p>
          </div>

          <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="font-bold text-gray-900 mb-2 flex items-center gap-2">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Stima Ordine Grandezza
            </h3>
            <p class="text-gray-600 text-sm">Timeline (sett/mesi), Budget (range), Complessità</p>
          </div>
        </div>

        <div class="mt-8 bg-white p-6 rounded-xl shadow-md">
          <p class="text-gray-700 font-semibold mb-2">Formato:</p>
          <p class="text-gray-600 text-sm">Documento PDF (5-10 pagine) o Google Doc condiviso</p>
        </div>
      </div>
    </section>

    <!-- Form Section -->
    <section class="py-16 px-6">
      <div class="container mx-auto max-w-2xl">
        <div class="bg-white p-8 md:p-12 rounded-2xl shadow-xl border border-gray-100">
          <h2 class="text-2xl font-bold text-gray-900 mb-6 text-center">
            Richiedi il Tuo Audit Asincrono
          </h2>

          <form id="audit-form" class="space-y-6">
            <div>
              <label for="name" class="block text-sm font-semibold text-gray-900 mb-2">
                Nome *
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-semibold text-gray-900 mb-2">
                Email *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
              />
            </div>

            <div>
              <label for="project" class="block text-sm font-semibold text-gray-900 mb-2">
                Nome Progetto *
              </label>
              <input
                type="text"
                id="project"
                name="project"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
              />
            </div>

            <div>
              <label for="objective" class="block text-sm font-semibold text-gray-900 mb-2">
                Obiettivo Principale *
              </label>
              <textarea
                id="objective"
                name="objective"
                required
                rows="4"
                maxlength="500"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
                placeholder="Descrivi cosa vuoi ottenere (max 500 caratteri)"
              ></textarea>
            </div>

            <div>
              <label for="materials" class="block text-sm font-semibold text-gray-900 mb-2">
                Link Materiali *
              </label>
              <input
                type="url"
                id="materials"
                name="materials"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
                placeholder="Link a GitHub, Google Drive, Dropbox, etc."
              />
              <p class="text-sm text-gray-500 mt-1">Repository, documenti, screenshot, o sample dati</p>
            </div>

            <div>
              <label for="notes" class="block text-sm font-semibold text-gray-900 mb-2">
                Note Aggiuntive (opzionale)
              </label>
              <textarea
                id="notes"
                name="notes"
                rows="3"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-600 focus:border-transparent"
                placeholder="Altre informazioni rilevanti"
              ></textarea>
            </div>

            <div class="flex items-start gap-3">
              <input
                type="checkbox"
                id="nda"
                name="nda"
                class="mt-1 w-5 h-5 text-green-600 border-gray-300 rounded focus:ring-green-600"
              />
              <label for="nda" class="text-sm text-gray-700">
                Il mio progetto richiede NDA prima della review
              </label>
            </div>

            <div class="flex items-start gap-3">
              <input
                type="checkbox"
                id="terms"
                name="terms"
                required
                class="mt-1 w-5 h-5 text-green-600 border-gray-300 rounded focus:ring-green-600"
              />
              <label for="terms" class="text-sm text-gray-700">
                Accetto che il feedback è fornito "as is" senza garanzie *
              </label>
            </div>

            <button
              type="submit"
              class="w-full px-8 py-4 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-all transform hover:scale-105 font-semibold shadow-lg"
            >
              Invia Richiesta Audit
            </button>

            <div id="form-status" class="hidden p-4 rounded-lg"></div>
          </form>
        </div>

        <div class="mt-8 text-center text-gray-600">
          <p class="mb-2">Preferisci parlarne direttamente?</p>
          <a 
            href="/prenota" 
            class="text-primary-600 font-semibold hover:text-primary-700"
          >
            Prenota una call gratuita →
          </a>
        </div>
      </div>
    </section>
  </main>
  
  <Footer />
</Layout>

<script>
  const form = document.getElementById('audit-form') as HTMLFormElement;
  const statusDiv = document.getElementById('form-status') as HTMLDivElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    submitBtn.disabled = true;
    submitBtn.textContent = 'Invio in corso...';

    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());

    try {
      const response = await fetch('/api/audit-request', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(data),
      });

      if (response.ok) {
        statusDiv.textContent = 'Perfetto! Ho ricevuto la tua richiesta. Ti rispondo entro 48 ore via email.';
        statusDiv.className = 'p-4 rounded-lg bg-green-100 text-green-800 border border-green-300';
        statusDiv.classList.remove('hidden');
        form.reset();
      } else {
        throw new Error('Errore durante l\'invio');
      }
    } catch (error) {
      statusDiv.textContent = 'Errore durante l\'invio. Contattami direttamente a mario@leoneconsulting.dev';
      statusDiv.className = 'p-4 rounded-lg bg-red-100 text-red-800 border border-red-300';
      statusDiv.classList.remove('hidden');
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = 'Invia Richiesta Audit';
    }
  });
</script>
